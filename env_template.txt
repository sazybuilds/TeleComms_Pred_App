#project root
PROJECT_ROOT="root/directory"

#dataset name
DATASET_NAME=dataset_name

#Output directories
MODEL_DIR=model_dir
LOG_DIR=logs

# files
MODEL_NAME="saved_model_pipeline.joblib"
LOG_NAME="app.log"

#training config
TARGET_COL="target_column_name"
TEST_SIZE=0.2
RANDOM_STATE=42



API_URL="port/predict"

# API Key — must be identical on both the backend (HF Spaces) and frontend (Streamlit Cloud).
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
API_KEY="your-secret-api-key-here"

